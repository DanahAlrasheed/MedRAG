# MedRAG
MedRAG is a Retrieval-Augmented Generation system for IoT-based clinical support. It collects data from devices like glucometers and heart rate sensors, converts it into structured queries, retrieves relevant medical evidence, and uses a Small Language Model to generate accurate, grounded explanations.
